import type { CalendarWeekDayType } from '../types/ui'

function CalendarWeekDay({ day, dayName, className, events, onClick, onEventClick, isToday }: CalendarWeekDayType) {
    return (
        <div>
            
        </div>
    )
}